# สั่งงาน AI: อ่านไฟล์ `AGENTS.md` ในโฟลเดอร์เดียวกันนี้ให้จบก่อนเป็นอันดับแรก

ในไฟล์ `AGENTS.md` มีชื่อผู้ใช้ อาชีพ และลักษณะงานที่เขาทำจริงเขียนไว้
ให้ใช้ข้อมูลนั้นตั้งชื่อบอร์ดและชื่อคอลัมน์ให้ตรงกับงานของเขา แล้วค่อยเริ่มสร้างตามเอกสารนี้

---

## 1. จะสร้างอะไร

เว็บแอพกระดานงานแบบ **Kanban** (คันบัง คือกระดานที่แบ่งเป็นช่องตามสถานะงาน
แล้วเลื่อนการ์ดงานจากช่องซ้ายไปช่องขวาเมื่องานคืบหน้า)

ใช้สำหรับจัดการงานของตัวเองเป็นหลัก หรือเปิดหน้าจอให้คนในทีมดูพร้อมกันก็ได้

---

## 2. ข้อบังคับทางเทคนิค (ห้ามฝ่าฝืน)

- ต้องเป็น **ไฟล์เดียวชื่อ `index.html`** จบในตัว มี HTML · CSS · JavaScript อยู่ในไฟล์นั้นไฟล์เดียว
- **ห้ามใช้ framework** (เฟรมเวิร์ก คือชุดเครื่องมือสำเร็จรูปอย่าง React หรือ Vue)
- **ห้ามมี build step** (บิลด์สเต็ป คือขั้นตอนแปลงโค้ดก่อนใช้งาน) และห้ามใช้ npm
- **ห้ามโหลดไฟล์ใดๆ จากอินเทอร์เน็ต** ไม่ว่าจะเป็น library · ฟอนต์ · ไอคอน · CSS จากเว็บนอก
  เพราะผู้ใช้ต้องดับเบิลคลิกไฟล์แล้วใช้งานได้ทันทีแม้เน็ตไม่ดี
  และต้องอัปโหลดขึ้น static hosting (โฮสต์ไฟล์นิ่งๆ) ได้เลยโดยไม่ต้องตั้งค่าอะไรเพิ่ม
- เก็บข้อมูลด้วย **localStorage** (พื้นที่เก็บข้อมูลในเบราว์เซอร์ของเครื่องผู้ใช้เอง)
  ไม่มีหลังบ้าน ไม่มีฐานข้อมูล ไม่มีระบบล็อกอิน

---

## 3. คอลัมน์ต้องปรับตามอาชีพผู้ใช้ (ข้อนี้สำคัญที่สุด)

**ห้ามใช้คอลัมน์สำเร็จรูปแบบ "To Do / Doing / Done" โดยไม่คิด**

ให้ทำตามลำดับนี้

1. อ่าน `CLAUDE.md` เพื่อดูว่าผู้ใช้ทำงานอะไร
2. ตั้ง **ชื่อบอร์ด** และ **ชื่อคอลัมน์ 3 ถึง 4 คอลัมน์** ให้เป็นภาษาที่คนทำอาชีพนั้นใช้จริง
   เช่น ถ้าเป็นแอดมินตอบแชท คอลัมน์ควรสื่อถึงขั้นตอนการดูแลลูกค้าจริงๆ
   ถ้าเป็นเจ้าของร้าน ควรสื่อถึงขั้นตอนของออเดอร์จริงๆ
3. ถ้าอ่านแล้วยังเดาไม่ออกจริงๆ ให้ถามผู้ใช้ **สั้นๆ ประโยคเดียว** ว่าทำงานอะไร
   ห้ามถามเป็นชุดคำถามยาว ห้ามถามเรื่องเทคนิค
4. ในโค้ดตรงจุดที่ประกาศชื่อคอลัมน์ ให้ **เขียนคอมเมนต์ภาษาไทยกำกับไว้ชัดๆ**
   บอกว่าแก้ชื่อคอลัมน์ตรงนี้ได้เลย เผื่อผู้ใช้อยากเปลี่ยนเอง

---

## 4. ความสามารถขั้นต่ำที่ต้องมีให้ครบตั้งแต่วันแรก

- เพิ่มการ์ดงานใหม่เข้าคอลัมน์ได้
- แก้ข้อความในการ์ดได้
- ลบการ์ดได้ และควรมีการถามยืนยันก่อนลบ เพราะกู้คืนไม่ได้
- ย้ายการ์ดข้ามคอลัมน์ได้
- ปิดเบราว์เซอร์แล้วเปิดใหม่ ข้อมูลทุกอย่างยังอยู่ครบ

---

## 5. ต้องใช้บนมือถือได้จริง

ผู้ใช้จะเปิดดูจากมือถือตัวเอง หน้าจอต้องปรับตามขนาดจอได้ (responsive)

วิธีย้ายการ์ดต้อง **ต่างกันตามขนาดจอ** เพราะบนมือถือการลากทำยากมากและมักพลาด

- **จอกว้าง (คอมพิวเตอร์)** ย้ายการ์ดด้วยการ **ลาก** เป็นหลัก
  และ **ซ่อนปุ่มย้ายซ้ายขวาไว้** ไม่ต้องแสดง เพราะทำให้หน้าจอรก
- **จอแคบ (มือถือ)** ให้ **แสดงปุ่มย้ายซ้ายและปุ่มย้ายขวา** บนทุกการ์ด
  ปุ่มต้องใหญ่พอกดด้วยนิ้วโป้งได้ ไม่ใช่ปุ่มจิ๋วที่ต้องเล็งด้วยเมาส์
- การสลับนี้ให้ทำด้วย CSS media query อย่างเดียว ไม่ต้องเขียน JavaScript ตรวจขนาดจอ
- บนจอแคบ คอลัมน์ควรเรียงต่อกันลงมาหรือเลื่อนดูด้านข้างได้ ไม่บีบจนอ่านไม่ออก

---

## 6. สิ่งที่ตั้งใจไม่ทำในเวอร์ชันแรก

เขียนไว้ให้ชัดเพื่อกันงานบานปลายจนทำไม่เสร็จในคาบเรียน
ถ้าผู้ใช้ไม่ได้สั่งเพิ่มทีหลัง **ห้ามทำสิ่งเหล่านี้**

- ระบบสมัครสมาชิกหรือล็อกอิน
- การให้หลายคนแก้ไขบอร์ดเดียวกันพร้อมกัน
- การแจ้งเตือน ไม่ว่าจะทางอีเมล ไลน์ หรือเบราว์เซอร์
- การแนบไฟล์หรือรูปในการ์ด
- มุมมองปฏิทิน
- การเชื่อมต่อกับบริการภายนอกทุกชนิด

---

## 7. หน้าตาของแอพ

**โทนสีขาวดำ ดูเรียบหรู** เป็นแกนหลักของหน้าตา

- ใช้ **ขาว เทา ดำ เป็นหลัก** ไม่ต้องมีสีฉูดฉาด
  ถ้าจะมีสีเน้น ให้มีสีเดียวและใช้น้อยมาก เช่นเฉพาะปุ่มหลัก
- ความหรูมาจาก **ที่ว่างเยอะ · เส้นบาง · เงาอ่อนๆ · ตัวอักษรจัดวางเป็นระเบียบ**
  ไม่ได้มาจากสีสันหรือลูกเล่น
- เขียน CSS เองทั้งหมด ใช้ฟอนต์ที่มีอยู่ในเครื่องอยู่แล้ว
- ตัวอักษรใหญ่พออ่านสบายทั้งบนคอมและมือถือ
- แต่ละคอลัมน์ควรเห็นชัดว่าแยกกัน และเห็นจำนวนการ์ดในคอลัมน์นั้น
- **ห้ามทำหน้าตาเลียนแบบธีม Windows 95** เพราะนั่นคือธีมของสไลด์สอน ไม่ใช่ของแอพนี้

**ต้องมีปุ่มสลับโหมดสว่างและโหมดมืด**

- วางปุ่มไว้มุมบนของหน้า กดแล้วสลับได้ทันที
- ทำด้วยการประกาศสีเป็นตัวแปร CSS แล้วสลับค่าที่ระดับบนสุดของหน้า
  ไม่ต้องเขียนสีซ้ำสองชุด
- จำค่าที่ผู้ใช้เลือกไว้ด้วย ปิดแล้วเปิดใหม่ต้องได้โหมดเดิม
- ครั้งแรกที่เปิด ให้ดูจากการตั้งค่าของเครื่องผู้ใช้ก่อนว่าเขาชอบโหมดไหน

---

## 8. เกณฑ์ตรวจรับ (ผู้ใช้เช็คเองได้ด้วยตา)

ถือว่างานเสร็จเมื่อผ่านครบทุกข้อนี้

1. ดับเบิลคลิกไฟล์ `index.html` แล้วบอร์ดเปิดขึ้นมาใช้งานได้ทันที โดยไม่ต้องติดตั้งอะไรเลย
2. ชื่อบอร์ดและชื่อคอลัมน์อ่านแล้วรู้สึกว่า "ตรงกับงานที่ฉันทำจริง" ไม่ใช่ชื่อกลางๆ
3. เพิ่มการ์ด · แก้ข้อความ · ลบการ์ด ได้ครบทั้งสามอย่าง
4. กดปุ่มย้ายซ้ายหรือขวา แล้วการ์ดย้ายคอลัมน์ได้จริง โดยไม่ต้องลาก
5. ปิดเบราว์เซอร์ทั้งหมด แล้วเปิดไฟล์ใหม่ การ์ดทุกใบยังอยู่เหมือนเดิม
6. เปิดบนมือถือแล้วอ่านออกทุกตัวอักษร **เห็นปุ่มย้ายซ้ายขวาบนการ์ด** กดโดนทุกปุ่ม ไม่ต้องซูม
7. ย่อหน้าต่างเบราว์เซอร์บนคอมให้แคบลง **ปุ่มย้ายซ้ายขวาต้องโผล่ขึ้นมาเอง**
   แล้วขยายกลับ ปุ่มต้องหายไปเอง
8. กดปุ่มสลับโหมดมืด แล้วทั้งหน้าเปลี่ยนสีทันที ปิดเปิดใหม่ยังจำโหมดเดิมไว้
9. ถ้าปิดอินเทอร์เน็ตแล้วเปิดไฟล์ หน้าตายังปกติครบถ้วน ไม่มีอะไรหาย
